package com.example.shared

/**
 * Placeholder shared logic (can be used by both Android and desktop if extracted).
 */
class Greeting {
    fun greet(): String = "Harness App - Desktop (KMP + Compose)"
}
