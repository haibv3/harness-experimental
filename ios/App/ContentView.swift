import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Harness App - iOS")
                .font(.largeTitle)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
